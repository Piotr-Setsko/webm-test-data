header.header
  h1._title #{data.page_meta.h1}
  img(src="#", alt="logo")._logo
  nav._nav
    ul._nav-list
        each item in data.nav
          li._nav-list-item
            a(href=item.href) #{item.text}